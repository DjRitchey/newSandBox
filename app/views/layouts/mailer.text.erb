<%= yield %>
<!-- ssh-keygen -t rsa -b 4096 -C "david@osa.io"

sudo apt-get install git-core curl zlib1g-dev build-essential libssl-dev libreadline-dev libyaml-dev libsqlite3-dev sqlite3 libxml2-dev libxslt1-dev libcurl4-openssl-dev software-properties-common libffi-dev


  To use in Apache: PassengerRuby /home/ritchey/.rbenv/versions/2.5.3/bin/ruby


  <VirtualHost *:80>
      ServerName ip-172-31-34-16

      # Tell Apache and Passenger where your app's 'public' directory is
      DocumentRoot /var/www/myapp/code/public

      PassengerRuby /home/ritchey/.rbenv/versions/2.5.3/bin/ruby

      # Relax Apache security settings
      <Directory /home/ritchey/apps/newSandBox/public>
        Allow from all
        Options -MultiViews
        # Uncomment this if you're on Apache >= 2.4:
        Require all granted
      </Directory>
  </VirtualHost> -->
